/**
 * Placeholder. The web app ships in BL-012 (decision between Next.js
 * via @cloudflare/next-on-pages vs. Hono+JSX on Pages).
 *
 * Until then this directory just exists so the monorepo workspace structure
 * is in place and `pnpm install` resolves cleanly.
 */
export const placeholder = 'web app scaffolding pending';
